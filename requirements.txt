PySide6>=6.7
ocrmypdf>=16.0
pyautogui>=0.9.54
mss>=9.0
img2pdf>=0.5
Pillow>=10.0
pymupdf>=1.27

# Mac専用: Apple Vision API binding
pyobjc-framework-Vision>=11.0; sys_platform == 'darwin'

# Windows専用: ウィンドウ操作（既に pyautogui の依存だが明示）
pygetwindow>=0.0.9; sys_platform == 'win32'
